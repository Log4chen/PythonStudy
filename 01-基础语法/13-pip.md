```
# 安装
pip install some-package-name
# 移除
pip uninstall some-package-name
# 已经安装
pip list

# 列出当前环境中已安装的所有 Python 包及其版本信息
pip freeze
# 导出到文件
pip freeze > requirements.txt

pip install -r requirements.txt
```